# ICLR Core100 Preparation Status

This file is the compact handoff checkpoint for preparation work. Detailed experiment truth remains in the external registries and receipts.

## Source baseline

```text
branch: feat/iclr-core100
preparation baseline commit: 1a9330d prepare ICLR Core100 experiment
```

## Milestone A — asset intake

State: `completed`

Completed:

- HomeInterior full Content bundle materialized in the workspace project;
- HomeInterior Map opened in UE 5.7 with 280 actors and promoted to Catalog `runtime_ready=true`, `local_preview`;
- user research-use/non-redistribution attestation recorded externally;
- warehouse_low identified as a Quixel Warehouse collection;
- all 57 FBX source IDs resolved through the public Megascans metadata endpoint with names, tags and authored dimensions;
- generated machine manifest: `registry/warehouse_low_source_manifest.json` under the external experiment root;
- generic UE StaticMesh scan and Catalog registration code added with unit tests.
- first user-terminal warehouse import materialized all 57 StaticMesh packages; 56 receipts passed and one Quixel metadata-size check produced a false negative;
- corrected the UE 5.7 AssetData object-path API and classified the downloaded Quixel collection as `external_site`; regenerated the deterministic 57-item batch.
- fixed rerun passed: HomeInterior scanned 102 StaticMeshes and registered all 102 in Catalog (100 collision-ready, 2 visual-only);
- regenerated warehouse import passed 57/57 with unique runtime-ready object bindings.
- warehouse scan passed 57/57 with collision, material and bounds evidence; all 57 were registered in Catalog;
- Catalog now contains 3254 assets, including 159 newly registered HomeInterior/warehouse meshes.

Remaining declared asset requests are limited to the specialized basket, bowling pin, funnel, spinning top, spring visual, and glass/wood fracture assets, plus the 15 frozen condition images. These are acquired when their cases approach execution rather than blocking the first controlled pilots.

## Milestone B — retrieval audit

State: `completed`

Completed:

- fixed SQLite `geometry_type` filtering so geometry shape is independent of backend asset class;
- removed arbitrary same-category fallback when there is no lexical or vector evidence;
- made retrieval fail closed with `no_semantic_evidence` when semantic channels cannot support a match;
- retained controlled `object_type -> subcategory -> category -> domain` relaxation and now records every taxonomy attempt;
- CaseSpec compilation demotes generated `must.category` and `must.physics_role` into taxonomy/preferences;
- generation guidance now defaults parent relaxation on and reserves exact semantic-class matching for explicit user requirements;
- changed multi-token FTS evidence from any-token to all-token, with a generic compact-name alternative for names such as `TrafficCone`;
- 76 focused retrieval/compiler/planning tests pass; the external benchmark rejects the no-match query correctly.

Post-intake verification:

- vector index rebuilt to `ready` with 3254/3254 text embeddings (2909 reused, 345 encoded);
- fixed seven-query benchmark passes with recall@8=1.0 and no-result accuracy=1.0;
- both English and Chinese semantic furniture queries return qualified HomeInterior chairs, while an unrelated axolotl query abstains.
- seven strict post-registration probes pass with recall@5=1.0 and no-result accuracy=1.0; new barrel/table/workbench/hand-truck assets resolve, while absent funnel/top/basket classes fail closed.

## Milestone C — roster and capability preparation

State: `in_progress`

Completed:

- replaced R034 with a rough-strip rolling slowdown case; no explicit inertia tensor or hollow-body support;
- replaced magnetic R073–R075 with centered impact, off-center impact, and spinning ballistic rod cases;
- recorded all four approved pre-freeze replacements in the external experiment manifest;
- revalidated the complete roster: 100 cases, exact domain/input quotas, readiness A=56/B=38/C=6;
- retained R071–R072 behind a generic continuous external-force capability;
- retain F010 as a later difficult rigid-SPH buoyancy target;
- do not add scenario-name or case-ID routing.
- passed the generic continuous-force real-UE smoke for R071–R072: UE was invoked, all 61 trajectory frames came from `adp_cpp_runtime_driver`, `bounded_push` appeared in 29 frames, and measured x displacement was 1.28999 m against a 0.3 m threshold;
- validated both 1280×720, 24 fps diagnostic videos; their low camera partially occludes the ball, so they are capability evidence rather than candidate-quality visual evidence.

Pending:

- prepare the later-case generic gates only when their first dependent cases approach execution: translating support, angular drive, rotating SPH boundary, rigid-SPH buoyancy, fracture, and attachment release.

Software implementation completed:

- CaseSpec V2 declares `continuous_force` with exact dynamic target, world-space Newton vector, and bounded time window;
- Runtime Compiler and local UE scene preserve this declaration without phenomenon-name routing;
- ADPPhysicsRuntime applies the force before each manual Chaos step and records the active force trace in every captured frame;
- 238 focused asset/retrieval/CaseSpec/compiler/runner/verifier tests pass (5 skipped); declared forces now require matching native force-trace evidence.
- the user-terminal UE 5.7 plugin build passed and activated package `ADPPhysicsRuntime_Mac_c78d614bc121`; the earlier managed failure was only AutomationTool log permission denial before compilation.
- the first user-terminal smoke attempt stopped before launching UE with `F4_UE_ACTOR_CLASS_MISSING`: its command omitted actor class, asset registry, and contact-export variables. The corrected complete environment now passes the read-only UE preflight; this is a launch-configuration failure, not a continuous-force result.
- froze the capability-first smoke order in external `registry/capability_smoke_plan.json`;
- the R061 generic smoke CaseSpec, runtime compilation, actorless world-anchor binding, delayed-release projection, and full UE preflight pass; five directly relevant constraint/verification tests pass. Real Chaos evidence is pending.
- a broader 167-test diagnostic run exposed two pre-existing/non-smoke assertion mismatches: Blueprint runtime `asset_kind` and a catalog-dependent domino collision-geometry source. They do not affect this smoke's compiled analytic bindings, but remain recorded for reconciliation rather than being counted as passes.
- R061 attempt 001 invoked real UE and released `driven_cart` at 0.4167 s after 21 held updates. The `spring_drive` native trace covered all 61 frames with correct 0.4 m translation, 0.8 m target and 3.2 J declared elastic energy, but Chaos reported 0 N throughout and the cart did not move. This is a generic drive-execution failure, not a verifier or release failure.
- fixed the generic lifecycle at commit `6849cdd`: after delayed release recreates a rigid-body physics state, the runner rebinds only constraints already declared against that body before applying release velocity. No case routing or fallback path was added; 98 related tests and the complete UE preflight pass.
- R061 attempt 002 passed in real UE: all 61 native trajectory and constraint-state frames were present, maximum elastic residual was `5e-9`, the cart moved 0.78238 m against the 0.2 m assertion, and both diagnostic videos were produced. The generic linear-drive/release gate is closed.
- prepared the F001 generic SPH solve/cache/UE replay smoke from the existing declaration-only `rigid_sph` path: strict CaseSpec validation and Runtime Compilation pass, the exact registered coffee mug and procedural tabletop resolve, and the compiled DAG is `genesis_sph:solve -> particle_surface_cache_v1 -> ue:render`. Genesis 1.3.2 and pysplashsurf 0.14.1.0 are installed; 103 related tests pass. Real execution is pending.
- F001 attempt 001 stopped at `F3_UE_MAP_UNRESOLVED` before Genesis or UE because the command requested unregistered Engine `Template_Default`. The strict map gate behaved correctly. Recompilation with qualified Catalog map `Map_BallBoardSmoke` passes; attempt 002 is prepared with only that launch correction.
- F001 attempt 002 passed end to end: Genesis verification passed, all 97 particle frames have reconstructed surface meshes, both `genesis_sph:solve` and `ue:render` completed, the harness verifier passed, and three diagnostic videos were published. The basic SPH solve/cache/replay gate is closed.
- prepared the A001 generic attachment plus root-turn smoke: strict CaseSpec validation, Runtime Compilation, actor placement, qualified-map resolution, and full UE preflight pass; the fixed mannequin mesh, physics asset, and Control Rig are materialized, and 43 related tests pass. Real UE evidence is pending.
- A001 attempt 001 invoked real UE and produced all 73 native frames plus three videos. All declared assertions passed (90 degree root yaw, 0.32069 m held-box x displacement, complete trajectory), but verification correctly rejected `frame_zero_evaluated=false`.
- diagnosed and fixed the generic high-resolution frame-zero lifecycle: synchronous `articulated_kinematic`/`articulated_ragdoll` initialization now remains ready, while only `articulated_character` waits for a world post-tick. No case routing or verifier relaxation was added; 79 related tests pass.
- A001 attempt 002 passed in real UE: all 73 articulated samples passed with frame-zero evidence, root yaw changed by 90 degrees, the attached box moved 0.32069 m on x, and all three diagnostic videos were published. All three pilot capability gates are now closed.
- froze pilot 01 `R001` at `2026-08-27T13:45:00+08:00`: the exact text request, final 9-second CaseSpec, six ordered baseball/step/ground contacts, terminal ground contact, trajectory integrity, seven object bindings, and `Map_BallBoardSmoke` are digest-locked under external `cases/R001/` with receipt `receipts/cases/R001/freeze_receipt.json`.
- the frozen CaseSpec digest is `69a7288a03e89e09d91635be8cad7668445de18de95d16bf3e655765202d53a3`; the corrected asset-lock digest is `a99138cc559b5b33e12c7158f3b3e384dc8933c97a1042ea908ab54900889563`. Read-only Catalog verification passes for all seven object assets plus the Map and is retained in external `reports/R001_freeze_validation.json`.
- historical `job_6a85651d_0cf3b3926710 / attempt_002` is recorded only as development evidence; it was not resumed or promoted. The freeze receipt records `candidate_jobs_created=0` and `historical_jobs_promoted=0`.
- the first pre-freeze projection omitted the Map license tier; it is preserved under `receipts/cases/R001/rejected_pre_freeze_001/` with an explicit rejection record. The accepted lock correctly records `local_preview` and does not overwrite the rejected audit trail.
- added a generic text-case freeze/validation path that verifies current CaseSpec V2, exact asset routes, qualified runtime bindings, Map qualification, immutable artifact digests, registry state, and status counts. Asset provenance hashes are recorded when available but do not block research execution. It is experiment control-plane tooling only and does not participate in runtime routing.
- completed development Job `job_iclr_pilot_r001_dev_001 / attempt_001` from the frozen R001 request and seed CaseSpec. It targets `local_preview`, ignores provenance/release gates per the research-first policy, made three UE launches, and is not an evaluated Candidate Job.
- R001 passed its automated real-UE verifier, render-sync, quality, and semantic-review gates in one attempt: 217 native frames, three 1280×720 views, ordered baseball contacts at frames `7, 24, 55, 94, 141, 193`, and no repair request. The user approved all videos on 2026-08-27; the formal Candidate counts remain zero.
- direct visual audit confirmed the ball is visible at declared contacts, especially in `side_static`; the plain diagnostic stage and black overhead geometry are capability-grade rather than paper-showcase quality.
- the pilot exposed a generic Evidence Bundle gap: selection ignored a declared `event_sequence` and centered the only `during` image on the later ground `event_exists`, after the subject left frame. Evidence selection now prefers a valid declared sequence and spans its interval; for R001 it selects frame 55 (step 3) as `during`. One hundred evidence/reviewer/controller tests pass, and UE does not need to be rerun.
- froze Pilot 02 `R013` as a new twelve-domino straight-chain CaseSpec: only `domino_01` has an initial 10-degree tilt, while eleven declared adjacent collision edges and an `event_sequence` assertion require Chaos-driven propagation through `domino_12`.
- R013 preparation compilation passes with 13 physics-critical actors, 12 simulated bodies, 11 collision edges, three cameras, exact reuse of the two registered local `box_mesh_v1` assets, and qualified HomeInterior Map `prepared_map.game_home_interior_maps_home_interior.b83a34674d53`.
- created clean development Job `job_iclr_pilot_r013_dev_001` from the frozen request and seed CaseSpec. A path-misconfigured duplicate under `jobs/jobs/` was cancelled before attempt creation, compilation, or UE launch and is retained only as an audit record.
- R013 development Job `dev_001` exposed a CaseSpec coordinate error: UE reads `rotation_deg` as `[Pitch, Yaw, Roll]`, so frozen `[0, -10, 0]` only yawed the first domino and all eleven contact assertions remained absent. The working spec now uses the intended Pitch `[-10, 0, 0]` with zero angular velocity; CaseSpec V2 validation passes, no runtime or verifier behavior changed, and the immutable first freeze remains as audit evidence.
- cancelled `dev_001` after the diagnosis rather than spend another UE launch on the known-invalid condition. The exact revision and both digests are recorded in external `reports/R013_development_revision_001.json`; formal Candidate counts remain zero.
- created clean replacement development Job `job_iclr_pilot_r013_dev_002` from the validated corrected working spec. It starts with zero UE launches and remains outside the formal Candidate set.
- R013 `dev_002 / attempt_001` passed smoke and Candidate execution with two real UE launches and no retries: 109 Candidate frames, three 1280×720 views, all eleven adjacent contacts in strict order at frames `9, 14, 18, 20, 23, 25, 27, 29, 31, 34, 36`, complete trajectories, render sync, quality hard gates, and runtime bindings all pass.
- the sole semantic Reviewer invocation passed with `repair_layer=none`, confirming only the first domino begins tilted and all twelve are down by about 1.75 seconds. Direct visual sampling finds `event_closeup` and `side_static` clear, while `overview` is heavily blocked by HomeInterior foreground geometry; R013 is therefore stopped at explicit human video review rather than starting Pilot 03.
- the user approved all R013 Candidate videos on 2026-08-27, accepting the documented weak overview for this development pilot. Pilot machine-pass and human-approval counts are both two; formal Candidate counts remain zero.
- with user approval, extended the experiment control-plane freeze path from text-only to generic `text`/`text_image` conditions. Image cases must provide at least one recognized image; each input is copied beneath the frozen case, identity/hash/size/MIME are included in the request and receipt, and registry validation checks both the binary and its confined path. Runtime and case routing are unchanged; two freeze tests and the current 100-case registry validation pass.
- prepared R011 from the supplied can-on-table image while retaining the roster's cylindrical rolling-body physics: a real warehouse_low rusty metal barrel rolls across a real HomeInterior dinner table, with hidden explicit cylinder/tabletop/floor collision geometry and three non-overview cameras. Exact asset and HomeInterior Map resolution, CaseSpec V2 validation, Runtime Compilation, and static support/contact checks pass after a 4 mm initial-height correction.
- with user approval, fixed the generic freeze asset-gate field mismatch: the tool now reads the same strict `pass`/`pass_local_preview` status from `qualification`, or from `quality_gate` when the former is absent. This covers imported HomeInterior/warehouse assets without changing runtime, thresholds, routing, or adding a case-specific fallback; both freeze tests and the current registry validation pass.
- froze R011 as the first `text_image` condition: the supplied PNG is copied under frozen `cases/R011/inputs/` and digest-locked together with the request, CaseSpec, four object assets, assertions, and HomeInterior Map. Registry validation passes with three frozen cases, and no formal Candidate Job was created or historical Job promoted.
- post-freeze compilation summary exposed an authored relation omission: the two-pair event sequence had only one explicit collision edge. The immutable first freeze is retained; external `reports/R011_development_revision_001.json` records the working-spec correction to explicit barrel-table and barrel-ground collision relations. Recompilation passes with exactly two asserted pairs and two collision edges; runtime and verifier are unchanged.
- created development Job `job_iclr_pilot_r011_dev_001` from the corrected working CaseSpec and frozen image identity. It starts with zero UE launches, no external Provider, no planning-model image upload, and remains outside the formal Candidate set.
- advancing R011 stopped at `generation` before any generation invocation or UE launch: the generic planning-image authorization gate runs before the existing seed-CaseSpec branch, although that branch validates the frozen image identity and performs no planning-model call. No image was uploaded. A minimal generic control-flow correction requires explicit approval before implementation: evaluate the upload gate only on generation paths that actually invoke a planning model; keep request-image identity validation unchanged.
- with user approval, seed generation now bypasses only the planning-model upload decision that cannot lead to an invocation; non-seed required-image planning remains gated. All 85 Job Controller tests pass and R011 resumed without image upload or generation calls.
- R011 `dev_001 / attempt_001` then used its first real UE launch and failed the smoke execution gate with `F_SEGMENTATION_QUANTIZATION_FAILED`. The missing color `(110,198,139)` maps to `ground`, whose compiled `visual_representation.visible` is explicitly false. The runner currently promotes every camera `target_object_id` to a required-visible segmentation instance, so it incorrectly requires pixels from a deliberately hidden collision proxy. This is a generic runner bug, not an R011 camera-layout failure; the failed Job and artifacts are preserved pending approval for a minimal visibility-filter correction.
- with user approval, segmentation required-visible selection now removes only actor bindings explicitly compiled with `visual_representation.visible=false`; full instance mapping, live-fracture requirements, visible targets, and unresolved-target checks remain intact. The focused regression passes, as do 74 segmentation/render-sync/quality tests. Two pre-existing unrelated assertions in the larger runtime-placement suite remain the already documented Blueprint asset-kind and domino collision-source inconsistencies.
- because `dev_001` is a non-retryable terminal execution failure, it remains immutable. Created clean replacement Job `job_iclr_pilot_r011_dev_002` from the exact same request digest and validated working seed CaseSpec; it begins with zero generation calls and zero UE launches, with all external/upload authorizations false.
- R011 `dev_002 / attempt_001` passed execution and the corrected segmentation gate, then failed the declared event sequence because the barrel remained on the tabletop: x displacement was `1.34362 m` and z displacement was effectively zero. Controller-authorized `attempt_002` raised the paired no-slip initial values from `1.10 m/s, -3.78 rad/s` to `1.65 m/s, -5.67 rad/s` without changing assertions or scene layout.
- `attempt_002` moved the barrel `2.13812 m` and it fell `0.45772 m`, proving it crossed the edge, but the 4-second window ended shortly before ground contact. The next bounded revision changes only scene duration from 4 to 5 seconds; current Job usage is two CaseSpec revisions and two UE launches.
- R011 `dev_002 / attempt_003` with the sole 5-second duration change passed smoke: 121 complete frames, barrel-table contact at frame 1, barrel-ground contact at frame 99, x displacement `3.01723 m`, and z displacement `-0.90632 m`. The validated working CaseSpec now mirrors the successful speed/duration revision; the first frozen version remains immutable audit history.
- the Controller stopped before Candidate with `candidate_budget_reserve_insufficient`: active runtime is `1114.46 / 1800 s`, CaseSpec revisions `3/5`, and UE launches `3/6`. Its reserve formula requires 900 seconds before Candidate; a 300-second extension is the smallest round extension that clears the 214.46-second deficit and leaves practical room for Candidate plus post-Candidate gates. Smoke will not be rerun.
- after explicit user approval, the 300-second extension raised soft/hard deadlines to 1920/2100 seconds and resumed directly at Candidate without rerunning smoke. R011 Candidate execution, declared assertions, render sync, quality gates, and Evidence Bundle all pass on `attempt_003`; the event frames and motion metrics exactly reproduce the successful smoke. Usage is four UE launches, three CaseSpec revisions, and zero generation/reviewer invocations.
- the explicit Review action stopped before invocation with `semantic_reviewer_image_upload_authorization_missing`, because R011 is image-conditioned and the isolated Reviewer input would include the original condition image. Reviewer usage remains zero and no image was transferred; the Controller requires a separate explicit authorization, after which review can resume without UE.
- the user granted ongoing `semantic_reviewer_image_upload` authorization for this experiment's remaining Pilot and formal Jobs. R011 resumed at Review without a UE rerun; its sole Reviewer invocation returned `semantic_intent_mismatch`. Native telemetry still proves tabletop motion and ground contact, but all three evidence views crop the first-ground-contact state and the requested WarehouseLow barrel renders as a featureless white cylinder.
- direct artifact diagnosis separates two visual defects. First, the imported WarehouseLow barrel mesh is correctly selected with qualified dimensions and 2172 source vertices, yet the collection import shares one `MatID_1` asset and the registered barrel has no materialized texture dependencies, despite the source bundle containing base-color, normal, roughness, and AO maps. Second, `bounds_auto_v1` frames only the initial scene bounds (`x` extent 0.65 m) while the verified barrel endpoint is about 3.02 m farther along positive X; the damped close-up additionally moves behind/into the table. This is not a physics or assertion failure, and semantic Review must not be resampled on the same attempt.
- no Harness source repair is required for R011. The current Catalog already contains qualified `external.polyhaven.barrel_03.c57f576af2df`, a weathered blue steel barrel with five complete UE mesh/material/texture dependencies and dimensions close to the current collision body. The research-first repair is therefore a normal working CaseSpec revision: select that existing visual asset and replace auto framing with explicit world-space fixed views covering the complete verified trajectory. Because the current Reviewer policy allows cameras but not an asset-request mutation, do not force this through the old attempt; preserve it and create a clean replacement development Job after approval.
- after user approval, the R011 working CaseSpec now selects the exact already-materialized `https://polyhaven.com/a/barrel_03` Catalog asset through `local_catalog` and replaces the cropped auto/event views with three explicit fixed world-space poses. Successful trajectory physics, 5-second duration, relations, and all four assertions are unchanged. Clean replacement Job `job_iclr_pilot_r011_dev_003` preserves the exact request/image digest, starts with zero usage, carries the experiment-wide semantic image-upload authorization, and requires no external Provider authorization.
- R011 `dev_003 / attempt_001` passed strict seed validation, exact Catalog resolution, smoke, Candidate execution, all declared assertions, render sync, quality gates, and Evidence Bundle with no retry: two UE launches, 121 Candidate frames, table contact from frame 1, first ground contact at frame 99, and zero generation/Reviewer invocations so far. Runtime binds exact `external.polyhaven.barrel_03.c57f576af2df` with all five texture/material dependencies, and the barrel now visibly reads as a blue steel drum. Direct evidence audit finds `side_static` clearly covers initial tabletop placement and landing, while HomeInterior walls block much of the explicit `front_static` and `overview`, and the automatically added targetless `event_closeup` remains cropped. The Job is honestly retained at `awaiting_semantic_review`; do not claim machine completion before its sole Reviewer result.
- R011 `dev_003` used its sole Reviewer invocation and failed at the observation layer, not physics or appearance: the structured evidence proves the table-contact episode ends at frame 93 and ground contact begins at frame 99, but the Evidence Bundle supplies `during=frame 1` and `after=frame 100`, so support loss and unsupported fall are absent from the review images. The Review exposes only `$.observation_requirements.cameras`, yet camera changes cannot alter those deterministic timestamps. The generic cause is `_select_event_points`: a two-pair `event_sequence` takes the first occurrence of pair one as its middle event. The smallest source correction is restricted to two-pair sequences with repeated contact episodes: select the last frame of the first episode, a gap midpoint, and the first frame of the second episode; leave three-or-more-pair behavior unchanged and add one regression. Per project policy this Harness fix awaits explicit approval before implementation.
- with user approval, the generic Evidence selection correction is implemented: a two-pair sequence selects the final old-support frame, unsupported midpoint, and first new-contact frame (R011: 93/96/99); longer sequences retain their existing event-span behavior, while non-sequence evidence uses deterministic uniform intervals. Evidence now uses at most two CaseSpec-declared views and at most twelve total images, with redundant montages removed.
- the approved research retention policy is implemented without changing Candidate RGB resolution or frame rate: smoke is 640x360; RGB-only runs skip depth, capture six uniformly spaced 640x360 diagnostic segmentation frames, and remove validated canonical sensor EXR after Evidence while retaining RGB, reports, metrics, and previews. Explicit depth/segmentation modalities retain their strict existing capture contract. The focused Evidence/Controller/runner/quality suite passes 183 tests.
- source commit `8674300` contains the approved Evidence/render-retention change. Clean replacement `job_iclr_pilot_r011_dev_004 / attempt_001` preserves the request digest, uses the revised unobstructed cameras, and passed smoke, Candidate physics/assertions, render sync, quality, and Evidence in two UE launches. Evidence selects frames 93/96/99 from `side_static` and `front_static`; validated EXR retention leaves the complete Job at 89 MB instead of roughly 10 GB.
- R011 `dev_004` used its sole Reviewer invocation and returned semantic fail: it accepts the roll-off/fall/landing sequence but sees a wide room shot without a clear tabletop-rolling phase. Direct video and actor-placement audit exposes two independent causes. First, `SM_Dinner_table` has local top at its actor origin, so `table_visual z=0` puts the rendered tabletop 0.84 m below the hidden collision tabletop at z=0.84 and makes the barrel appear to roll in midair. Second, the new transition Evidence correctly covers support-end/fall/new-contact but contains no earlier supported-motion frame, so no camera-only revision can prove tabletop rolling to Reviewer. The smallest next work is a generic fourth `supported_motion` Evidence point for sustained two-pair sequences (still nine total images for this image-conditioned case), plus a CaseSpec revision aligning the visible tabletop and adding one closer table-edge camera. Reviewer supplied no suggested adjustments, so Controller correctly stops at `needs_user_decision`; both the Harness correction and new attempt require explicit approval.
- with user approval, source commit `e7364b1` adds the generic sustained-support Evidence point. The Controller-created R011 `dev_004 / attempt_002` aligns the HomeInterior floor, table mesh, hidden tabletop collider, and barrel from measured UE runtime bounds: floor/collision top is exactly `102.082383 cm`, table visual bottom differs from it by only `0.017889 cm`, visible/collision tabletop differs by `0.017899 cm`, and the barrel bottom is exactly tangent to the tabletop. Smoke, Candidate execution, every assertion, render synchronization, quality, sparse Evidence, and semantic Review all pass. Evidence uses supported-motion/support-end/unsupported/new-contact frames `47/93/96/99` across two views; the Reviewer reports `overall_status=pass`, `repair_layer=none`, and no suggested adjustments. The Job is machine-complete with four UE launches total and is stopped for explicit human video approval; do not start Pilot 04 yet.
- the user rejected R011 `dev_004 / attempt_002` at human review. The foreground table/barrel scale and placement do not agree with the HomeInterior kitchen perspective: the barrel reads oversized, the table reads suspended, and the visible barrel intersects the rendered tabletop while rolling. Post-review audit shows why the prior numeric claim was insufficient: both real assets use `preserve_authored`, all three declared cameras observe from the same negative-Y side, and the exact tangent belongs to the analytic cylinder/table proxy rather than a calibrated visible-shell contact. Preserve the completed machine-pass Job as audit history. No Harness source change is justified yet; prepare a clean development Job from a CaseSpec that explicitly scales the visible assets and matching analytic colliders, seats the table into the Map floor, and uses elevated orthogonal/three-quarter views. Inspect the smoke frames manually before Candidate.
- the approved R011 visual repair is materialized in working CaseSpec digest `44bb1011146f84b5d6ae8e8d93f24e57b37cc48c45e199dbf1b60358957dd8c3`: the table uses a `0.75` uniform fit, the barrel a `0.60` uniform fit, their analytic proxies are resized consistently, the table feet extend 2 cm into the qualified Map floor, the barrel begins one scaled radius plus 1 cm above the visible tabletop, and its `1.3 m/s` / `-6.7857 rad/s` pair preserves no-slip rolling. Three revised elevated/three-quarter views are declared. Clean Controller Job `job_iclr_pilot_r011_dev_005` preserves the original request digest and semantic image-upload authorization and remains at `created` with zero UE launches. The public Controller currently has no stage-boundary option: `advance-until-blocked` proceeds directly from a passed Smoke into Candidate. Do not use an unaudited timed interrupt; choose explicitly between a small generic stop-after-stage control or allowing this development run through Candidate.
- approved source commits `31c0ddc` and `45e700b` add audited `--stop-after-stage` boundaries to both ordinary advance and resume-with-revision; 87 Controller tests pass. R011 `dev_005 / attempt_001` stopped at compile with zero UE launches because the planned 1 cm settling gap violated the explicit support relation's 2 mm tolerance. The exact compiler-suggested 1 cm downward correction is CaseSpec digest `abc367590afd7c5b2848313dd0ecf3625a78cd45ad6791c634bb3958632afc87`; Controller attempt 002 then passed compile and the real 640x360 Smoke in one UE launch and stopped before Candidate as requested. Telemetry proves the analytic barrel bottom and tabletop top are both `163.095 cm`, but direct frame review still fails visual alignment: the visible table bounds are `131.589–194.602 cm`, while the Map floor is `102.082 cm`. Thus the table floats by `29.507 cm` and its visible top sits `31.507 cm` above the collision top, making the correctly tangent barrel look embedded. The cause is generic: Actor Placement compiles a non-procedural support asset's position as its top surface and emits a `-half_height` visual-center offset, while native UE's `bounds_center` pose alignment currently ignores that compiled offset and puts the bounds center at the declared top. The minimal repair is to apply the existing compiled visual-center offset when deriving the bounds-center target; add a regression, revise only the table visual center if required by the corrected compile contract, and rerun Smoke at the audited boundary. This Harness source repair awaits explicit approval.
- with user approval, source commit `bf524b6` makes native UE `bounds_center` placement apply the compiler's existing `visual_local_bounds_center_from_collision_m`; it adds no case routing, new anchor, or fallback. The focused native-placement and Controller suites pass 116 tests. Because `dev_005` had already crossed Smoke, clean replacement `job_iclr_pilot_r011_dev_006 / attempt_001` preserves the exact request and corrected working CaseSpec, passes compile and one real 640x360 Smoke, and stops at the audited pre-Candidate boundary with one UE launch. Runtime bounds now put the table feet at `100.082 cm` against floor top `102.082 cm`, visible tabletop and collision top at `163.095 cm`, and barrel bottom at `163.095 cm`. Direct sampling across both declared views confirms the table is seated, the barrel does not intersect it, and the roll-off remains visible. Await explicit human approval of these Smoke videos before Candidate; do not start Pilot 04.
- the user accepted dev006 physics but rejected its remaining foreground/background scale. The approved research-only CaseSpec revision uniformly scales the visible table, barrel, and matching analytic collision bodies by `0.75`, scales no-slip linear speed to `0.975 m/s` while retaining angular speed, moves the assembly `0.30 m` toward open floor, and moves all three cameras back with moderate fields of view. The vertical fall assertion is correspondingly scaled from `0.60 m` to `0.40 m`; contact ordering and trajectory integrity are unchanged. Working CaseSpec file digest is `7d32cfe8fa2cd3c131dc62d5faa260aa8892078a933d037b72df4fad1890b45f`.
- clean replacement `job_iclr_pilot_r011_dev_007 / attempt_001` preserves the exact request digest, passes compile and one real Smoke with no retries, and stops before Candidate after one UE launch. Runtime bounds put the table feet at `100.082 cm` against floor top `102.082 cm`, table visual/collision top and barrel bottom at `147.342 cm`. Direct samples show a roughly `1.26 m` table and `0.29 m` barrel with visible floor contact, no tabletop penetration, and complete roll-off motion. It awaits human judgment of background coordination; do not start Candidate or Pilot 04 yet.
- the user rejected dev007 background coordination. A read-only environment audit finds only two materialized, Catalog-usable Worlds: the rich but unsuitable `Home_Interior` and the plain diagnostic `BallBoardSmoke`. `Home_Interior_Overview.umap` exists on disk but is not qualified as a prepared runtime Map. The `warehouse_low` source contains 57 loose FBX props and no `.umap` or `.uproject`, so it is not a replacement warehouse scene. Other Catalog Worlds are unmaterialized blocked discovery records. R011 remains stopped before Candidate pending the user's choice between a plain/no-rich-background presentation and importing another complete environment Map.
- at the user's request, source commit `e56c13b` adds a reusable Poly Haven HDRI acquisition/UE Map preparation path without changing CaseSpec or Runtime routing. Thirty explicit `HDRIs/interiors` assets, including `lebombo`, `empty_workshop`, `ballroom`, and `wrestling_gym`, are downloaded as MD5-verified 2K HDR files under external `PhysicsAssetSource/polyhaven/hdris/interiors/` (189 MB) with a manifest. All 30 are imported as TextureCube assets and paired with HDRIBackdrop prepared Maps in one 138 MB external UE Content bundle. Each Map uses the same explicit origin, 15 m projection dome, cubemap SkyLight, and soft directional key; no case identity selects a special execution path.
- `empty_workshop` is the first qualified pool Map. Its final Catalog identity is `prepared_map.game_polyhavenhdri_maps_empty_workshop.eb1f943f2d3c`, Map hash `ca1f95ad96d0ea4c0168108c8020567d81461ceee6cb0c3ff0e66aa668a1ffc0`, and real UE qualification loads exactly one HDRIBackdrop, one movable SkyLight, and one movable DirectionalLight. The superseded no-light Catalog row `...0f46d816d256` was deleted exactly after read-only resolution proved it shadowed the final asset; no source HDR, UE asset, historical Job, or current Map was deleted. The other 29 generated Maps remain ready for on-demand Catalog qualification after this visual representation is accepted.
- the background change is recorded as a new R011 request digest rather than silently violating the original HomeInterior request. `dev_008 / attempt_001` proved the HDRI projection and physics in one Smoke but exposed missing foreground illumination: the background rendered correctly while the table and barrel were nearly black. The generic prepared Map lighting addition was then applied to all 30 Maps. Clean `job_iclr_pilot_r011_dev_009 / attempt_001` resolves only the final qualified Map, passes compile and one real Smoke with no retries, and stops before Candidate after one UE launch. Direct samples show restored wood/metal materials, a central open workshop, continuous projected floor, correctly seated table feet, and unchanged roll-off motion. It awaits explicit human video approval; do not start Candidate or Pilot 04.
- the user approved `dev_009` Smoke. The same attempt then passed Candidate execution, every declared assertion, render synchronization, automated quality, and the sparse Evidence Bundle with two total UE launches and no retries. Direct sampling of `side_static`, `front_static`, and `overview` confirms sustained tabletop rolling, support loss, free fall, first ground contact, correct foreground contact, and coordinated Empty Workshop scale. The sole semantic Reviewer invocation returned `uncertain` only because result images cannot prove the exact HDRI file identity; it explicitly accepts the requested motion and event sequence and suggests no adjustment (`repair_layer=evidence`). The Controller is therefore honestly retained at `needs_user_decision`; do not revise physics, rerun Reviewer, or start Pilot 04. The next decision is whether the user's standing research-first instruction makes this non-visual identity-only uncertainty non-blocking, followed by human Candidate video review.
- the user explicitly accepted all R011 `dev_009` Candidate videos and directed identity, provenance, and authorization concerns not to block the research. The experiment therefore records Pilot 03 as human-approved while preserving the Controller's truthful `needs_user_decision / semantic_review_uncertain` state; the Controller is not relabeled completed, the Reviewer is not resampled, and no physics or Evidence revision is created. Pilot 04 R010 may proceed.
- Pilot 04 R010 now has an Agent-authored text-only CaseSpec and clean Controller Job `job_iclr_pilot_r010_dev_001`. It combines the qualified HomeInterior Map and real dinner-table visual with generic procedural stand, rod, ball, block, analytic tabletop collision, a passive hinge, and a fixed bob attachment. Strict CaseSpec V2 validation passes. The first Controller compile stopped before Smoke at `F3_invalid_initial_physics_state`: each stand post overlaps the crossbar by 25 mm. Asset resolution consumed one UE importer launch but no simulation/render launch. The smallest allowed revision moves only the two post centers from `y=±0.30 m` to `y=±0.37 m`, keeping them on the visible tabletop while clearing the crossbar; it awaits explicit approval before a new attempt.
- the approved R010 stand-post revision was applied through the Controller. `attempt_002` passes Runtime Compilation, and R010 is frozen with its exact request, revised CaseSpec, qualified assets/Map, assertions, and condition receipt. The full 100-case registry validates with frozen IDs `R001/R010/R011/R013`; no formal Candidate Job was created.
- R010 `dev_001 / attempt_002` then used its first simulation/render launch and failed Smoke at `F_SEGMENTATION_QUANTIZATION_FAILED`: `side_static` has no pixels for segmentation color `(216,141,23)`, which maps exactly to `table_visual`. This is an authored observation/layout failure, not a physics failure. Native telemetry records a real pendulum-ball/target-block hit at `0.4167 s` with `0.62157 N·s` impulse and target-block +X displacement `0.28149 m`. Diagnostic RGB frames show the tabletop cropped below the cameras and the loaded HomeInterior anchor looking into an empty sky/background region, making the apparatus appear unsupported. Preserve the successful physics and assertions. The smallest research-first revision is to use a prepared open Poly Haven HDRI Map and lower/widen the three fixed cameras so the complete real table is visible; keep only actual story subjects in each camera's required-visible target list. No Harness source change or verifier relaxation is justified.
- with user approval, `lebombo` was registered and qualified as prepared Map `prepared_map.game_polyhavenhdri_maps_lebombo.eb1f943f2d3c`; real UE loads one HDRIBackdrop, one movable SkyLight, and one movable DirectionalLight. The R010 working CaseSpec switches only its environment/observation layer, lowers and widens the three fixed cameras, removes `table_visual` from required-visible targets, and aligns the non-colliding real table top to the unchanged analytic tabletop. Clean replacement Job `job_iclr_pilot_r010_dev_002 / attempt_001` preserves the original request digest and passes compile plus one real Smoke with one UE launch and no retry. All assertions and render sync pass: native ball-block contact occurs at `0.4583 s` with `0.65070 N·s` impulse, the block moves `0.30138 m` along +X, and visible/collision tabletop mismatch is only `0.000066 cm`. Direct sampling finds all three fixed views clear, the full table grounded, the background scale coordinated, and the collision response visible. Stop before Candidate for explicit human Smoke review.
- the user requested a small R010 presentation/physics refinement after reviewing `dev_002`: keep the crossbar flush with, but not beyond, the post exteriors; lift the pendulum clear of the tabletop; retain the ball-block collision; and slightly lower tabletop friction. Because friction was outside the running Job's bounded revision policy, clean replacement `job_iclr_pilot_r010_dev_003 / attempt_001` preserves the request digest and applies the change in a new audited CaseSpec rather than bypassing the Controller.
- R010 `dev_003` passes compile and one real Smoke and stops at the audited pre-Candidate boundary. The 0.79 m crossbar spans exactly between post outer edges `y=±0.395 m`, and its bottom is tangent to both post tops. Native telemetry records zero ball-table contacts and a minimum clearance of `0.024609 m`; the ball still strikes the block at `0.458333 s` with `0.676112 N·s` impulse, moving it `0.38314 m` along +X. All assertions and render sync pass. Direct sampling confirms the complete stand, continued late swing, clear impact response, grounded table, and coordinated Lebombo background. Await explicit human Smoke video approval before Candidate.
- the user approved R010 `dev_003` Smoke. Candidate execution then completed with four valid 1280×720 videos and the automated verifier passed, but the quality gate correctly stopped at `F_CONTACT_AT_INITIAL_FRAME`: the source CaseSpec declares `target_block supported_by table_surface` and redundantly declares the same pair as a future `collision`, so the initial load-bearing contact appears at frame 0 in the expected collision graph. This is an authored semantic-relation error, not a physics or Harness failure. The minimal correction is to remove only that redundant `collision`, preserving the support relation and ball-block impact. Because relations are outside this Job's bounded revision policy, preserve `dev_003` and create a clean replacement Job only after explicit approval.
- the user reviewed and approved all R010 `dev_003` Candidate videos and accepted the metadata-only quality exception. R010 is recorded as the fourth human-approved development pilot; Controller truth remains `needs_user_decision / quality_gate / F_CONTACT_AT_INITIAL_FRAME`, no machine-complete label is fabricated, and no rerun is required. Formal Candidate counts remain zero.
- a proposal-only title/contract audit estimates the current Core100 difficulty mix at roughly 29 atomic, 52 medium, and 19 high cases versus the desired 20/55/25. R018 Newton's cradle, R015 bowling, and R063 bungee already exist. The recommended pilot change promotes them to Pilots 05, 08, and 07 respectively while retaining R036/R016/R061 in the formal roster. External `reports/core100_difficulty_audit_001.md` proposes nine same-quota replacements for redundant atomic rigid cases; neither roster nor pilot order has been changed pending approval.
- the user approved the complete difficulty proposal. The external roster now replaces R006/R012/R023/R031/R037/R053/R054/R056/R059 and changes Pilots 05-08 to R018/R048/R063/R015; R036/R061/R016 remain formal Core100 cases. Registry validation passes with 100 cases, exact 80/10/10 domain and 85/15 input quotas, readiness A/B/C `51/43/6`, and contiguous Pilot order. The approved titles and before/after identities are preserved in `experiment_manifest.json` and external `reports/core100_roster_change_receipt_001.json`.
- Pilot 05 R018 needs no external Newton-cradle asset for development: five procedural balls, five rods, a generic frame, the qualified real table, and Empty Workshop Map form the physical/visual scene. Clean Controller Job `job_iclr_pilot_r018_dev_001 / attempt_001` compiled and ran real Smoke with two UE launches. All ten constraint residual checks, all four adjacent contact assertions, trajectory/artifact integrity, and repeated forward/return transfers pass. First transfer impulses are `0.800/0.736/0.678/0.624 N·s`; the last ball reaches `x=0.62902 m`, a `0.25902 m` positive excursion.
- R018 Smoke stops only because the authored `state_delta >= 0.10 m` measures final-minus-initial position after six seconds (`0.05129 m`) rather than peak swing. The smallest correct generic assertion is `state_value(position_m.x, reduction=max) >= 0.47 m`, preserving the same 10 cm physical threshold; no layout, runtime parameter, Harness source, or verifier behavior changes. Assertion paths are outside `dev_001`'s bounded revision policy, so preserve it and create clean `dev_002` only after explicit approval.
- The user reported that the original 5 mm gaps let the middle balls visibly join the repeated motion and supplied `/Volumes/TiPlus7100s/PhysicsAssetSource/materials/scratched_phosphate`. The package is a packed scratch/roughness imperfection mask, not a complete metal material: it has no BaseColor or Normal and its glTF metallic factor is zero. Current CaseSpec has no generic texture-material input, so the texture is retained for a later general material pipeline rather than adding case-specific Runtime behavior.
- Approved clean `job_iclr_pilot_r018_dev_002 / attempt_001` changes the resting centers to exact visual tangency at 0.18 m spacing, declares the three initial pairs as `contact`, uses the corrected last-ball peak assertion, and adds non-relaxed ±3 cm bounds for each middle ball. Strict CaseSpec V2 validation passes. Real UE Smoke uses one launch; all ten constraint checks, all four contact assertions, artifact/trajectory integrity, and the last-ball peak pass. The last ball reaches `x=0.51543 m`, a 0.15543 m excursion.
- R018 `dev_002` truthfully stops at `needs_user_decision / smoke / declared_assertion_failed`: simultaneous rigid contacts make the resting chain move substantially as a block. Balls 02/03/04 span `[-0.30027,-0.12223]`, `[-0.07479,0.12617]`, and `[0.11912,0.30639]` m respectively. Do not relax these assertions. The smallest source-only next experiment is a clean `dev_003` retaining visually tangent 0.18 m spheres while reducing only their analytic collision diameters to 0.178 m, introducing a 2 mm sequential-impact/compliance proxy without Harness source changes. It requires user approval before materialization.
- The user approved R018 `dev_003`. Its strict CaseSpec retains 0.180 m tangent visual spheres and changes only the five analytic colliders to 0.178 m. Clean `job_iclr_pilot_r018_dev_003 / attempt_001` compiles and runs one real UE Smoke with no retry. All contacts, constraints, artifacts, trajectories, and the last-ball peak pass; the last ball reaches `x=0.54886 m`, a 0.18886 m excursion. The strict middle-ball assertions still fail: balls 02/03/04 span `[-0.28470,-0.10382]`, `[-0.07077,0.07532]`, and `[0.10742,0.31312]` m. A 2 mm gap is therefore insufficient and must not be accepted by relaxing thresholds.
- With user approval, source commit `716adbb` adds one generic UE primitive: explicitly paired sphere bodies receive an equal-and-opposite spring-damper normal force only while compressed; separation produces zero force. CaseSpec must declare each pair, activation distance, stiffness, and damping, and the hard collision spheres remain only a smaller safety stop. The focused contract/runtime tests and real UE 5.7 plugin build `ADPPhysicsRuntime_Mac_8e6dfcd0a52c` pass.
- Clean `job_iclr_pilot_r018_dev_004 / attempt_001` keeps all 13 strict R018 assertions, uses exact 0.180 m visual tangency, 0.174 m hard collision spheres, four 0.180 m compliant contacts, 360 Hz, `k=200000 N/m`, and `c=5 N*s/m`. It compiles and launches real UE once, then terminates at Smoke with Controller-classified Harness bug `F_RUNTIME_CONSTRAINT_ENFORCEMENT_FAILED`. The run is numerically unstable rather than marginal: just before impact ball 01 moves at about 1.36 m/s; one sample later multiple balls reach 20-28 m/s and every cradle constraint is pulled apart. The first locked residual is 0.145621 m at 0.5 s and the maximum pivot residual reaches 2.120683 m. Preserve this failed Job as the capability-development reproduction; do not relax assertions or retry it.
- Source commit `df3ae13` adds the approved generic explicit-step rejection, and 93 CaseSpec/generation tests pass. Subsequent isolated two-sphere Jobs `job_iclr_smoke_compliant_contact_v1_001` through `_005` disprove the initial assumption that a smaller declared stiffness alone is sufficient. Jobs `_001`/`_002` show the same ±20 m/s energy injection at `k=50000 N/m`; `_003` confirms the CaseSpec rate is now carried as 360 Hz by commit `d792e9c`, but the callback still behaves at the outer render frame. The temporary repeated-World-Tick experiment in `9989b1b` is fully removed by `d43a52c` after `_004` proves only the first of 15 same-frame ticks advances Chaos. Job `_005` proves the custom-physics callback receives/integrates the full 1/24 s outer frame even when the reported runtime timebase is 360 Hz. All failed Jobs are preserved; none is retried or used as a production R018 attempt.
- The compact UE custom-force route therefore cannot supply the stiff per-substep unilateral spring R018 needs. Do not create R018 `dev_005`, relax its 13 assertions, or expose the current experimental `compliant_contact` as passed. The smallest viable replacement is a generic unilateral instantaneous impulse contact: for explicitly paired approaching spheres at the activation distance, solve the normal impulse from the two masses, relative normal velocity, and declared restitution, apply equal-and-opposite impulses, and emit the same contact telemetry. This is a model change from compliant spring to rigid impulse and requires explicit approval; it must use no case IDs, runtime clamp, or semantic routing.
- The user subsequently chose the simpler research-first R018 baseline: retain the analytic unilateral sphere-impulse route only as a documented deferred alternative, and accept small middle-ball motion under ordinary Chaos contacts. Clean `job_iclr_pilot_r018_dev_005 / attempt_001` restores the already proven `dev_001` geometry (0.180 m visual/collision spheres at 0.185 m resting center spacing, i.e. 5 mm gaps), removes every compliant-contact declaration, and replaces only the incorrect final-minus-initial assertion with `max(ball_05.position_m.x) >= 0.47 m`. Smoke, Candidate verifier, render synchronization, quality gate, and Evidence Bundle all pass with two UE launches, no retries, and no Reviewer invocation. The Controller is truthfully paused at `awaiting_semantic_review`; await human Candidate video approval before Reviewer or Pilot 06.
- The user approved every R018 `dev_005` Candidate view. Its sole authorized semantic Reviewer invocation then returned `uncertain` only because rendered evidence cannot prove that the deferred analytic impulse model was disabled; it explicitly judged the scene and motion substantially matched and proposed no adjustment. Preserve the truthful Controller state `needs_user_decision / semantic_review_uncertain`, do not revise or resample, and record R018 as Pilot 05 human-approved under the standing research-first rule. Pilot 06 R048 may proceed as a text-only development Job while its final evaluated condition image is still missing.
- Pilot 06 R048 now has an Agent-authored text-only development CaseSpec: a three-layer/two-block-per-layer wooden tower, ordinary box contacts, and one generic 6 N horizontal `continuous_force` on the front lower block from 0.8–1.4 s. The new `wrestling_gym` prepared Map is registered as `prepared_map.game_polyhavenhdri_maps_wrestling_gym.eb1f943f2d3c`; real UE loads its HDRIBackdrop, SkyLight, and DirectionalLight, and Catalog qualification passes. `job_iclr_pilot_r048_dev_001 / attempt_001` stops before UE at compile with `F3_invalid_initial_physics_state`: all seven procedural boxes incorrectly declare `fit_uniform_to_approx_size`, while exact-size procedural geometry requires no instance scaling. The corrected working CaseSpec removes only those seven redundant keys and passes V2 plus strict Agent validation. Because scale policy is outside dev001's bounded revision paths, preserve it and create clean dev002 only after approval; do not change geometry, force, assertions, or runtime code.
- Approved clean `job_iclr_pilot_r048_dev_002 / attempt_001` compiles and runs one real UE Smoke with no retry. The prepared Wrestling Gym background, cameras, initial tower stability, trajectories, and force telemetry are valid; `pull_lower_block` records 14 native `[-6 N]` samples over the declared 0.8–1.4 s window. Smoke stops only at `declared_assertion_failed` because the target lower block moves `-0.00088 m` instead of the required `-0.35 m`: the force is below the combined static friction from the platform and two load-bearing upper contacts. No Harness change or assertion relaxation is justified. The smallest physically coherent allowed revision keeps all six blocks equal and changes only their masses from 0.55 kg to the policy minimum 0.275 kg, reducing normal/friction load while preserving geometry, friction, force, timing, and verification; await approval before applying it to the same Job.
- The approved equal-mass R048 revision is recorded as `job_iclr_pilot_r048_dev_002 / attempt_002`: all six blocks are 0.275 kg while geometry, friction, the 6 N force, timing, cameras, and assertions remain unchanged. One additional real UE Smoke passes render synchronization and every declared verifier check with no retry. The force trace contains all 14 samples; by 1.4 s the pulled block has moved about 1.85 m along -Y, the tower visibly collapses, and both the pulled block and one upper block subsequently fall beyond the finite stage. Direct samples of all three RGB views show a stable initial tower, coordinated Wrestling Gym background, clear removal/collapse, and no initial penetration. The Controller is intentionally stopped at the completed Smoke boundary before Candidate; await human review of the three Smoke videos. The final evaluated R048 run still requires its frozen condition image, but that does not block this text-only development pilot.
- The user accepted R048 physics and requested one clean visual comparison: hide the visible platform while retaining its analytic collision, apply Poly Haven `oak_wood_planks` to the six blocks, and raise all six equal masses slightly to 0.32 kg. Invisible collision is already a normal CaseSpec `visual_representation={source:none, visible:false}` declaration, and mass is already supported. The exact texture exposes one narrow generic capability gap: the UE Runner already consumes `visual_material_path`, but CaseSpec V2 projection does not preserve that source field, while the governed local importer accepts only OBJ/FBX rather than texture materials. Do not bypass the Controller or patch R048. Await approval for a small general material-reference/import path, then create clean `job_iclr_pilot_r048_dev_003`; preserve dev002 unchanged as the accepted physics baseline.
- Approved source commit `59d2904` adds only generic CaseSpec validation/projection for qualified `/Game/...` `visual_material_path` values; the existing UE Runner remains unchanged. Focused CaseSpec-to-Runtime tests pass. Poly Haven's CC0 `oak_wood_planks` 1K diffuse, DirectX normal, and ARM textures are stored on the external asset disk and successfully materialized in the UE project as `/Game/PolyHavenMaterials/OakWoodPlanks/M_OakWoodPlanks_1K`. Clean `job_iclr_pilot_r048_dev_003 / attempt_001` keeps the accepted request and physics structure, hides only the stage visual, applies the shared oak material to all six blocks, and sets all six masses to 0.32 kg. Compile and one real UE Smoke pass with no retries: render sync, 14 force samples, 121 trajectory samples, removal, collapse, and artifacts all pass. Direct samples confirm visible wood grain, no visible platform, plausible alignment to the Wrestling Gym mat, and clear collapse in all three views. The pulled block's final -Y delta is 16.95565 m versus 30.8895 m in dev002, so the launch is materially less violent. Stop before Candidate for human Smoke review.
- The user approved all R048 dev003 Smoke views. The same Controller Job then completed Candidate with one additional UE launch and no retry. All three high-resolution RGB views exist, and the Candidate verifier repeats the passing 14-sample force trace, 121-frame trajectory, lower-block removal, upper-block drop, and artifact checks. The Controller is intentionally stopped at the completed Candidate boundary before Quality Gate; await human Candidate video review.
- The user accepted R048 dev003 Candidate with one explicit formal-run note: the hidden finite support is too small, so late falling blocks leave it. Preserve this Pilot result and do not rerun it. Before the formal Core100 R048 freeze/run, enlarge the invisible collision support to cover the visible landing region (or bind a qualified prepared-Map floor collision) while keeping it visually hidden and leaving the tower composition unchanged. Quality Gate and Evidence Bundle pass. The single authorized semantic Reviewer returns `pass`, cites stable initial motion through 0.6667 s, delayed lower-block extraction after 0.8 s, and visible loss of support/collapse by 1-2 s, and suggests no adjustment. Controller truth is `completed`; Pilot 06 is both machine-passed and human-approved with two UE launches and zero retries.
- Pilot 07 R063 is the approved text-only `bungee_ball_drops_and_rebounds` contract (`slack_fall_stretch_and_damped_rebound`), not a mannequin case. It needs no external object asset: a procedural ball, hidden anchor, existing dynamic `axial_visual` cord, and a qualified open HDRI Map are sufficient. The user approved the smallest general capability addition: optional `soft_linear_limit={stiffness_n_m,damping_n_s_m}` on an already-limited rigid constraint. The first implementation maps directly to Chaos `SetSoftLinearLimitParams`, carries native parameter readback/telemetry, and allows expected soft overshoot without weakening hard-limit checks. There is no case-ID dispatch, alternate solver, restitution/contact-distance option, or new constraint type. Focused tests pass; the relevant full test groups pass, apart from the two pre-existing Blueprint/domino Actor Placement baseline mismatches. Universal Mac plugin package `ADPPhysicsRuntime_Mac_879e8564d646` builds and activates successfully, but physical qualification below is still required before claiming slack/rebound support.
- Real UE qualification rejects the native Chaos soft-limit execution despite successful bind/readback. The controlled smoke tries stiffness/damping `45/3`, `500/10`, `2000/30`, and `5000/50`; both zero and UE-standard 5 cm contact distance are tested. Every physically strict run exceeds the 1.0 m limit by several metres (`3.38-6.17 m`) or reaches the remote floor. One earlier automated pass is explicitly invalidated because its upward velocity came from floor contact; the corrected height lower-bound assertion fails. Do not freeze or create R063 from these artifacts and do not continue parameter sweeping. The smallest truthful next route keeps the same generic CaseSpec field but implements tension-only `k*extension + c*separation_speed` through the existing ADP substep/add-force infrastructure, with no case-ID/type dispatch, while leaving ordinary hard limits and bilateral drives unchanged. This is a materially different implementation from the approved native-parameter route and therefore requires user approval; alternatively revert the unqualified capability and replace/defer R063.
- A real UE timing A/B rules out missing Chaos substeps as the cause. The existing manual capture advances the PIE World by a fixed `1/24 s` while runtime PhysicsSettings enable five `1/120 s` substeps; its first post-step payload velocity is `-59.85 m/s`. An otherwise identical 24 Hz probe disables substepping and still produces `-39.18 m/s`, whereas gravity alone should contribute only about `-0.41 m/s` over that interval. Explicitly re-entering `World->Tick` five times from the current Driver callback stalls UE and is not an acceptable scheduler implementation. The native Soft Limit failure therefore remains a constraint implementation/semantics issue rather than evidence of an unfixed render/physics timebase. Source was restored cleanly; no runtime change was committed.
- A second real UE probe disables Driver manual stepping entirely, makes no Harness `World->Tick`/`advance_capture` calls, unpauses the PIE World, and lets the Driver record only from its ordinary Actor Tick. The first effective Chaos update reproduces the exact manual-path state (`payload z=0.2039 m`, `vz=-59.85 m/s` from `z=2.0 m`), and the next reaches the remote floor. The headless Editor cadence is coarse (`0.4 s` game-time jumps), although PhysicsSettings still clamp each physics update to `1/24 s` with five `1/120 s` substeps; this rules out the explicit manual Tick call itself, but does not claim equivalence to a smooth interactive Editor session. All probe-only source changes were reverted and the inner worktree is clean.
- The approved replacement removes the unqualified native `soft_linear_limit` contract entirely and adds one generic `unilateral_distance_spring={rest_length_m,stiffness_n_m,damping_n_s_m}` constraint field. It requires all three linear axes to remain free and cannot coexist with a linear drive. At each actual Runtime force evaluation it applies only tension, `T=max(0,k*max(0,d-L0)+c*v_separation)`, as equal-and-opposite impulses at the authored constraint-frame points; it supports world-to-rigid and rigid-to-rigid endpoints and contains no case, object-type, or semantic routing. Telemetry records the actual evaluated distance, extension, speed, tension, direction, force, and cumulative evaluation counters rather than recomputing a post-step force.
- The compact custom-physics callback is invoked once per fixed `1/24 s` Harness World update on this UE path, not once per internal Chaos substep. A rejected experiment queued five same-frame World updates and produced five callback evaluations but only one solver advance, slowing gravity by about five times; it was fully reverted. The final implementation reports the observed evaluation rate truthfully and does not add a second scheduler or Chaos async callback. This rate is sufficient for the bounded R063 parameters but is not presented as a general stiff-contact solver.
- Final plugin `ADPPhysicsRuntime_Mac_fd210781f601` builds successfully. Strict real UE capability smoke `unilateral_distance_spring_v1 / attempt_005` passes with 97 trajectory samples, 95 force evaluations (`23.75 Hz`), 83 active-tension evaluations, maximum force-law residual `1.297e-6 N`, and maximum elastic-energy residual `5e-9 J`. The payload falls to `z=1.06192 m`, rebounds upward at `1.676798 m/s`, and never contacts the remote floor. `attempt_004` is only an operator-timeout-aborted PIE startup and contains no physics result. The seven related regression modules run 314 tests with 312 passing and only the two already recorded Blueprint asset-kind/domino Catalog-collision baseline mismatches.
- Initial native Controller Job `job_6a9161be_fab490df2172` is preserved as a zero-Smoke authored-tier failure: its default `reference` target conflicted with five generated `local_preview` assets, so Provider qualification stopped after one importer launch. Under the standing research-first/provenance-nonblocking instruction, clean replacement `job_iclr_pilot_r063_dev_001` uses `local_preview`, compiles successfully, resolves the qualified Wrestling Gym Catalog Map and all five procedural assets, then stops on its first Smoke UE launch at `F_UE_NATIVE_SCRIPT_EXCEPTION`. The authoritative log shows the actual generic defect: Asset Resolve selected `/Game/PolyHavenHDRI/Maps/wrestling_gym.wrestling_gym`, but Runtime Case retained the source Catalog asset ID `prepared_map.game_polyhavenhdri_maps_wrestling_gym.eb1f943f2d3c`; native scene passed that invalid ID to `load_level`. No physics frames or video were generated. The approved minimal fix now projects the selected Map's qualified UE object path into Runtime Case immediately after the single Asset Resolve, without changing the immutable source CaseSpec, adding fallback, or recognizing this Map/case. A focused Catalog-ID integration test and all 143 Runtime Compiler/Asset Resolver/Controller regressions pass. Both failed Jobs remain immutable; proceed with a clean `dev_002` after committing the source fix.
- Commit `ab3480f` lands that generic Map binding fix. Clean `job_iclr_pilot_r063_dev_002 / attempt_001` compiles and resolves the qualified Wrestling Gym Map, then passes its real UE Smoke: 121 trajectory frames, 119 unilateral-spring evaluations (`23.8 Hz`), 107 active-tension evaluations, maximum force-law residual `9.16e-7 N`, maximum energy residual `5e-9 J`, minimum ball height `1.31623 m`, and maximum upward velocity `1.679939 m/s`. All declared assertions, render synchronization, and Smoke artifacts pass. Operator error used `advance-until-blocked`, which continued past the intended human Smoke boundary through Candidate, Quality Gate, and Evidence Bundle; those machine stages also pass, but no Semantic Reviewer was invoked. Controller truth is `awaiting_semantic_review`, with two UE launches and zero retries. Treat the three Smoke RGB videos as the current review target and do not invoke Reviewer or start Pilot 08 until explicit human approval.
- The approved R063 appearance adjustment is complete without changing its force law or assertions. Poly Haven `hessian_230` 1K diffuse, DirectX normal, and ARM maps are stored under the external asset source and materialized as `/Game/PolyHavenMaterials/Hessian230/M_Hessian230_1K`; the material is explicitly enabled for spline meshes. Commits `a64a8f4`, `1605db6`, `344f2a7`, `9cb14ea`, `9205e2d`, and `2108af4` implement one generic axial-visual path: derive slack from the declared unilateral spring rest length, refresh the visual during trajectory replay, bind the declared material to spline rendering, and render the quadratic cord as eight short spline-mesh segments so a procedural cylinder can visibly bend. There is no case ID, object-type, or semantic routing and no new CaseSpec field.
- Diagnostic Jobs `dev_003` through `dev_010` are retained as visual-development evidence only; `dev_010` confirms that the HomeInterior `SM_Wire` mesh is unsuitable because its authored cross-section renders as a wide ribbon. Clean `job_iclr_pilot_r063_dev_011 / attempt_001` returns to the procedural cylindrical cord, uses Hessian 230, passes one real UE Smoke, and stops exactly at the audited post-Smoke boundary with one UE launch and no retry. Its verifier reproduces the accepted physics: 121 samples, 119 evaluations (`23.8 Hz`), 107 active-tension evaluations, maximum force residual `9.16e-7 N`, maximum energy residual `5e-9 J`, minimum ball height `1.31623 m`, and maximum upward velocity `1.679939 m/s`. Direct front-view samples show a curved slack cord initially, a straight taut cord during descent, and renewed curvature during rebound; runtime logs contain no missing spline-material/default-material warning. No Candidate, Quality Gate, Evidence Bundle, or Semantic Reviewer was run for dev011.
- The user approved all dev011 Smoke views. The same attempt then completed one 1280x720/24 fps Candidate UE launch and stopped exactly at the audited post-Candidate boundary. Candidate verification reproduces all 121 trajectory samples and the same spring/energy residuals, minimum height, and upward rebound; direct sampling confirms the slack/taut/rebound cord states and Hessian material at paper-review resolution. Controller truth is `blocked / requested_stage_boundary_reached`, current stage `quality_gate`, with two UE launches, zero retries, and zero Reviewer invocations. Quality Gate, Evidence Bundle, and Reviewer have not run.
- The user approved all three dev011 Candidate videos. Quality Gate and the sparse Evidence Bundle then pass without another UE launch. The sole authorized Semantic Reviewer returns `semantic_intent_mismatch`: it accepts the complete slack/stretch/rebound/damped motion but claims Hessian 230 and the Poly Haven HDRI are absent. This is a proven Evidence-summary false negative, not a scene defect: the immutable CaseSpec and Runtime Case bind `/Game/PolyHavenMaterials/Hessian230/M_Hessian230_1K.M_Hessian230_1K`, Studio Runtime Scene carries the same material, Asset Resolution resolves `https://polyhaven.com/a/wrestling_gym`, and Runtime/Studio Scene open `/Game/PolyHavenHDRI/Maps/wrestling_gym.wrestling_gym`. The Evidence summary's `explicit_asset_bindings.items` is empty because it collects object Provider bindings but not direct qualified material paths or the selected prepared Map; its `selected_environment` is likewise null.
- Under the standing research-first rule that identity/provenance metadata must not block, and the user's direct Candidate approval, record Pilot 07 R063 as machine-physics-passed and human-approved. Preserve Controller truth as `needs_user_decision / semantic_intent_mismatch`, two UE launches, zero retries, one Reviewer invocation; do not revise, resample, or claim the Job itself is `completed`. The generic Evidence metadata omission is recorded for later correction but does not justify delaying Pilot 08.
- previously approved storage cleanup removed only depth/segmentation EXR intermediates from R011 `dev_001` through `dev_003`, reducing them from 1.1 GB / 542 MB / 10 GB to 8.3 MB / 58 MB / 32 MB. Job manifests, videos, reports, Evidence, and Reviewer audit remain; deleted intermediates are recoverable only by rerunning those historical Jobs.

Capability-first real smokes:

- before pilot 7: linear drive/release path for R061;
- before pilot 9: selected-branch basic SPH solve/cache/UE replay for F001;
- before pilot 10: attachment plus root-turn path for A001;
- passed before later evaluated cases: continuous force R071–R072;
- still required before their later evaluated cases: translating support R050–R051, angular drive R066/R070, rotating boundary F009, rigid-SPH buoyancy F010, fracture R079, and attachment release A010.

## Next exact action

Pilot 07 R063 is closed as a human-approved development Pilot with the Reviewer false negative preserved. Await explicit instruction before preparing Pilot 08.
